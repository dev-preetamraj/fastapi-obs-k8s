# Version control
.git
.gitignore
.gitattributes

# Python build/runtime artifacts
.venv
__pycache__
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Tooling caches
.ruff_cache
.mypy_cache
.pytest_cache

# Editor / IDE
.vscode
.idea

# Tests not needed in the image
tests/

# Agent / docs not needed in the image
# (README.md is kept — hatchling reads it at build time per pyproject.toml)
AGENTS.md
CLAUDE.md
TODOS.md
.claude

# Docker files themselves (not needed inside the image)
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.*.yml
compose.yml
compose.*.yml

# Python version marker (uv reads requires-python from pyproject.toml)
.python-version
